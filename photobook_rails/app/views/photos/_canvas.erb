<script>
  let img;

  console.log(settings);

  function preload() {
    img = loadImage('<%= (rails_storage_proxy_path(photo.image))%>');
  }

  function setup(){
    var canvas = createCanvas(<%= @width %>,<%= @height %>).parent("#p5canvas");
    image(img, 0, 0);
  }

  function draw(){
  }
</script>
